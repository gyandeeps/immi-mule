import React from "react";
import { Layout } from "antd";

const { Footer } = Layout;

const AppFooter: React.FC = () => {
    return <Footer className="immi-footer">footer</Footer>;
};

export default AppFooter;
